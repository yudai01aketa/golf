<div id="list-<%= @course.id %>" class="list-button">
  <% if !current_user.nil? && current_user.list?(@course) %>
    <%= render 'users/unlist' %>
  <% else %>
    <%= render 'users/list' %>
  <% end %>
</div>
