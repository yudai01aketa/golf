<div id="favorite-<%= @course.id %>">
  <% if !current_user.nil? && current_user.favorite?(@course) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
